# API Reference

This section is generated by DocFX from the source XML documentation comments (`docfx metadata`).

Run `docfx metadata` then `docfx build` to populate it. By default it indexes the core **RinkuLib** project. To include the analyzers or PowerTools projects, add them to `docfx.json` > `metadata` > `src`.
