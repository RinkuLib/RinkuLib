# API Reference

This section is generated by DocFX from the projects' XML documentation comments (`docfx metadata`).

Run `docfx metadata` then `docfx build` to populate it. By default it indexes the core **RinkuLib** project; add the analyzers and PowerTools projects to `docfx.json` > `metadata` > `src` to include them too.
